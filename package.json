{
  "name": "three_vite",
  "version": "0.0.4",
  "author": "fdoganis",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "purge": "npm run clean && rm -rf node_modules",
    "serve": "http-server dist",
    "deploy": "rm -rf dist && vite build && gh-pages -d dist",
    "cloud": "cloudflared --url http://localhost:5173/"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "vite": "^5.4.1",
    "vite-plugin-glsl": "^1.3.0",
    "vite-plugin-static-copy": "^1.0.6"
  },
  "dependencies": {
    "@tweenjs/tween.js": "^25.0.0",
    "browser-sync": "^3.0.3",
    "gh-pages": "^6.2.0",
    "three": "^0.169.0",
    "three-mesh-ui": "^6.5.4",
    "zzfx": "^1.3.0"
  }
}
